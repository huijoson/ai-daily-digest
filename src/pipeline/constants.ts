/** Max content images included per article (extraction cap + summarizer guard). */
export const MAX_ARTICLE_IMAGES = 12;

/** Max age for a Hacker News article to be shown / summarized (24h). */
export const HN_MAX_AGE_MS = 24 * 60 * 60 * 1000;
